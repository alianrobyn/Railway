@using Railway.Data.Models
<MudDialog>
    <DialogContent>
        <MudStack>
            <MudTextField @bind-Value="@_ticketType.Name" Label="Назва" Required/>
            <MudNumericField @bind-Value="@_ticketType.AdditionCost" Label="Додаткова вартість" Required Format="F2"/>
        </MudStack>
    </DialogContent>
    <DialogActions>
        <MudButton OnClick="Cancel">Скасувати</MudButton>
        <MudButton Color="Color.Primary" OnClick="Submit">Створити</MudButton>
    </DialogActions>
</MudDialog>

@code {
    [CascadingParameter] IMudDialogInstance MudDialog { get; set; } = null!;

    private readonly TicketType _ticketType = new();

    private void Submit()
    {
        MudDialog.Close(DialogResult.Ok(_ticketType));
    }

    private void Cancel()
    {
        MudDialog.Close(DialogResult.Cancel());
    }

}
